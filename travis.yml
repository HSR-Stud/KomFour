before_install:
- sudo apt-get update
- sudo apt-get install -y texlive-latex-extra texlive-lang-german texlive-math-extra
script:
- pdflatex -interaction=nonstopmode ./IntTra.tex
- pdflatex -interaction=nonstopmode ./IntTra.tex
deploy:
  provider: releases
  api_key: $GITHUB_RELEASE_TOKEN
  file:
   - ./IntTra.pdf
  skip_cleanup: true
